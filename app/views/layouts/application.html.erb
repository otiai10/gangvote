<!DOCTYPE html>
<html>
<head>
  <title>MAN of the GAME</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <!--link rel="stylesheet" href="bootstrap/css/bootstrap.css" /-->
  <meta name="viewport" content="width=device-width,initial-scale=1,user-scalable=no">
  <link rel="shorcut icon" href="/assets/logos/icon.png" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <link rel="apple-touch-icon" href="/assets/logos/icon.png" />
</head>
<body style="background-image:url('/assets/game/<%= @game.hometeam %>_<%= @game.awayteam %><%= IMG_EXTENTION %>');">
<!-- twitterbootstrap navbar -->
<div class="navbar navbar-inverse <% if(controller_name != 'top') %>navbar-fixed-top<% end %>">
  <div class="navbar-inner">
    <%= link_to(raw('MAN<span class="sml"> of the GAME</small>'), {:controller => 'top'}, {:class => 'brand'}) %>
    <ul class="nav pull-right">
      <!-- li class="active"><a href="#">Home</a></li>
      <li><a href="#">Link</a></li>
      <li><a href="#">Link</a></li -->
      <li>
        <div class="login-info">
          <p></p>
          <% unless @user_name.nil? %>
            <%= link_to(raw('<span class="xsml">' + @user_name + 'として参加中</span>'), {:controller => 'players'}, {:class => 'brand'}) %>
          <% end %>
          </a>
        </div>
      </li>
    </ul>
  </div>
</div>
<!-- /twitterbootstrap navbar -->
<div class="contents">
<% if(controller_name != 'top') %><div id="top-space"></div><% end %>
<%= yield %>
<hr>
<div class="footer">
  <p class="xsml">copyright otiai10. All Right Reserved.</p>
</div>
</div>
</body>
</html>
