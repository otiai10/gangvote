<div class="row">
  <div class="span9">
    <p>あなたの「ええやん！」がチームの力に</p>
    <p class="lead">
      みんなで決める<br>
      <b>M</b>ost<br>
      <b>E</b>eyan<small class="xsml">~ええやん~</small><br>
      <b>P</b>layer
    </p>
  </div>
</div>
<div class="row">
  <div class="span9 game-title">
  <% if @user_name.nil? || @game_join.nil? %>
    <%= form_tag({ :controller => 'account', :action => 'login' }, { :id => 'ini-form' }) do %>
      <%= @game.title %>
      <input type="hidden" name="game" value="<%= @game.hometeam + '-' + @game.awayteam %>">
      <%= text_field_tag 'user_name', '', { :size => 50, :placeholder => 'ユーザ名を登録', :required => true } %><br>
      <%= submit_tag '参加', { :class => 'btn'} %><br>
      <small>※ MVP投票集計以外の目的には使用されません</small><br>
      <small>※ このユーザ名は10時間後に破棄され無効になります</small>
    <% end %>
  <% else %>
    <h3><%= @user_name %>::参加中</h3>
    <h4><%= link_to('Players', {:controller => 'players'}) %></h4>
  <% end %> 
  </div>
</div>
